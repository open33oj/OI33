# OI33 — 33OJ Unified Hydro Plugin

把多个 legacy 插件（coin、birthday、badge、realname、checkin、pastebin、猫罐头经济、奖章/拍卖、AI 题意/审核、wiki、OAuth 等）整合为单个 Hydro addon。入口 `index.ts` 依次调用各 `handler/*.ts` 的 `apply(ctx)`；model 统一经 `model/index.ts` 的 `oi33Model` 桶导出，业务数据全部存在 `oi33_*` 集合（字段见 `model/types.ts`）。

## 目录

- `model/` 数据层：`user.ts`（oi33_user 主档：签到/猫粮/实名/徽章）、`cat-*.ts`、`school-cat.ts`、`medal.ts`（奖章定义 + `medalCategoryOf` 派生四系列 + 可升级等级阶梯（奖项认证奖章与 OJ 成就奖章共用 `levels`，自动系列每级带自己的 `threshold`）+ 发放/等级 API，集合 `oi33_medal` / `oi33_user_medal`，`oi33_user_medal.level` 记录所持等级；原 `achievement*` 符号全部改名：`medalList`/`medalCatalogue`/`medalGet`/`medalSave`/`medalDelete`/`medalGetUserAwards`/`medalListRecentAwards`/`medalListAwardsPaginated`（管理页“最近授予”分页，返回 `{awards,total,tpcount}`）/`medalAwardStats`/`medalListLevelHolders`（某认证等级当前持有者，供目录点入）/`medalGrant`/`medalSetLevel`/`medalRevoke`/`medalEvaluateUser`/`medalEvaluateAll`/`medalImportInitialDefinitions`/`medalExportDefinitions`/`medalImportDefinitions`（奖章定义 JSON 导入 / 导出，仅定义不含持有记录）/`medalCategoryOf`/`medalCategoryName`/`medalGroupByCategory`/`medalLevelOf`（严格查询，写路径用）/`medalDisplayRung`（宽松查询，仅渲染用：等级被管理员从阶梯中删除后回退到最接近的存活等级）/`medalSortedLevels`/`medalAwardView`/`medalIsLevelSeries`/`medalThresholdLevel`/`medalAutomaticRuleText`/`medalMigrateAutomaticLevels`/`AUTOMATIC_RULE_TYPES`/`MEDAL_AUTOMATIC_SERIES`/`medalGetAcceptedDomains`/`medalSetAcceptedDomains`/`medalAcceptedDomainIncluded`/`ensureMedalIndexes`）、`auction/contract.ts`（`oi33_auction` / `oi33_medal_contract`，均以 `medalId` 引用奖章；拍卖助手改名 `auctionSaleableShowcase`、`remainText` 移入本模块；`contractDelete` 仅允许发起人删除已取消的合同）、`ai.ts`（AI 配置/用量/批量状态 `oi33_ai_batch`）、`moderate.ts`（审核队列 + `normalizeText`/`hashOf`/`bioHashOf`/`sameBioText`/`bioQueueState`/`modExpireEntries`）、`algorithm.ts`（算法掌握：`oi33_algorithm_item` 大纲/自定义项目 + `oi33_user_algorithm` 一人一项的 4 档评定（没学/不会/不熟/熟练，无记录按没学显示，前端用洛谷难度色号着色）+ `oi33_algorithm_config` 单文档配置；默认大纲随插件内置在 `noi-outline-2025.json`（NOI 2025 大纲，3 级/12 板块/260 项，含难度 1-10；`ALGORITHM_DROPPED_SECTION` = 官方「基础知识与编程环境」板块，已从大纲剔除）；`algorithmProfileView`/`algorithmGroupItems`/`algorithmComputeStats` 产出面板视图与统计，`algorithmSetLevels` 写评定，`algorithmMonthKey`/`algorithmNextUpdateDate`/`algorithmGetSelfQuota`/`algorithmClaimSelfQuota`/`algorithmReleaseSelfQuota`/`algorithmResetSelfQuota` 管学生每月一次的提交额度，`algorithmImportOutline`/`algorithmEnsureOutlineImported` 幂等导入（`normalizeOutlineId` 把官方 `2.1.1-12` 规范成 `1.1.12`，`algorithmMigrateOutlineIds` 迁移库内旧 id），`algorithmSaveItem`/`algorithmDeleteItem`/`algorithmSetItemEnabled`/`algorithmBulkSetEnabled` 管理项目，`algorithmNormalizeLevel`/`ALGORITHM_LEVELS` 为等级常量）、`log.ts`（审计日志，每次写操作都记）。
- `handler/` 路由与逻辑（`ctx.Route` 注册，路由表看代码）：`user/content/admin/profile/wiki/oauth/cat-*/school-cat/ai/moderate/meow/medal/auction/contract/bio.ts`（`medal.ts` 注册 `/oi33/medals*` 全部奖章路由——管理、保存、配置、定义导出 `/oi33/medals/export` 与导入 `/oi33/medals/import`、授予、`/oi33/medals/level` 等级升降、撤销、扫描、公开目录（认证系列每个等级可点入 `/oi33/medals/catalogue/:id/level/:level` 查看该等级持有者）、展示柜、用户奖章页；四系列由 `medalCategoryOf` 派生，另注册 `/oi33/achievements`、`/oi33/achievements/rare`、`/oi33/achievements/showcase`、`/oi33/achievements/user/:uid` 四条旧路由的 GET 重定向）；`contest.ts` 注册 OC（One Chance）赛制（`ContestModel.RULES.oc`，基于 core `ioi` 规则 `buildContestRule`，按每题个人时限内第一次提交计排名分、括号显示全部比赛内提交的最高分，编辑页选项靠 `templates/contest_edit.html` 里的 `contest-rule--oc` class 与 IOI 对齐，`templates/contest_detail.html` 追加赛制规则卡片，文案在 `locales/zh.yaml`「Contest Rules」节；比赛列表标签颜色在 `frontend/contest-oc.css`，core 只为内置赛制配色故 `contest-type--oc` 需自带背景；`templates/contest_main.html` 与 `templates/partials/homepage/contest.html` 同名覆盖 core，把 /contest 顶部大卡片、全部比赛列表（含历史）与首页比赛组件统一为紧凑表格（面板卡片内每赛两行三列：状态图标 rowspan+大字号名称链接/赛制·起止时间·时长+右侧跨两行居中的参加人数，无参加按钮，宏在共享的 `templates/partials/oi33_contest_table.html`，样式在 `frontend/contest-main.css`））。**OC 补题**：个人时限（startAt+duration，封顶比赛 endAt 与提前结束 tsdoc.endAt，见 `ocDeadline`）之后——无论比赛是否结束——都可继续在比赛内提交，记录仍挂 contest 进 journal 但被标记 `late`，不计排名分只计括号最高分。实现：包装 `ProblemSubmitHandler.prototype.prepare`（OC 已参赛用户放行 `ContestNotLiveError`）与 `ProblemDetailHandler.prototype._prepare`（赛后保持 `mode='contest'` 使提交 URL 带 tid）；core 的 `updateStatus`/`revPushStatus` 不懂个人 deadline 且会重建无标记 entry，故 `handler/after/ProblemSubmit#post` 与 `record/judge` 两个事件触发 `recalcOcStatus` 重打 `late` 标记并重算 score/detail/display（持久化标记保证 core 的 recalcStatus/unlockScoreboard 重算结果也正确））；`patches.ts` 所有 monkey-patch；`utils.ts` 的 `checkUserFlag`/`checkOi33Admin`；`algorithm.ts` 注册 `/oi33/algorithm`（跳转本人展示页，`?uid=` 供老师打开他人）、`/oi33/algorithm/user/:uid`（展示页：只读、知识点标签云、级别/板块/子板块逐层折叠）与 `/oi33/algorithm/user/:uid/edit`（评定页：每行单选框 + 按评价/关键词筛选）、`/oi33/algorithm/set`（单表单整份面板一次批量提交；学生每次提交消耗当月唯一额度、老师随时可存）、`/oi33/algorithm/reset-month`（flag≥2 重置某学生本月额度），以及 `/oi33/algorithm/manage*`（flag≥2 的项目增删改、启停、批量、导入、全局设置）；`handler/algorithm.ts` 不再往 UserDetail 注入 `oi33AlgorithmPanel`，资料页不内嵌算法掌握卡片，只在侧边栏「快捷跳转」里给入口。
- `templates/` Nunjucks，同名覆盖 core（`user_detail.html`、`components/user.html`、`layout/html5.html` 等）；`user_detail.html` 主列不再有头像/头图，依次展示个人简介（默认固定高度折叠）/奖章（只展示展示柜，其余收进「全部奖章（N）」折叠区）/学习记录（通过题目 + 最近活动 + 最近题解合成一个 section，空的部分不渲染）/注册信息（管理员），侧边栏用 `partials/oi33_profile.html` 的 `userCard`（头像、昵称、身份徽标、第二行的 AtCoder / Codeforces 主页链接、提交/通过/RP，**快捷跳转直接接在卡内**：个人页面/站内消息/最新喵喵/算法掌握/全部奖章/硬币账单 + 分割线 + 编辑个人信息/编辑 OI33 资料，`quickNav` 已并入 `userCard` 不再单独成卡，喵喵页的关注按钮仍通过 `{% call %}` 追加在卡底部），喵喵与算法掌握只在快捷跳转的独立页、不内嵌资料页；资料页**不渲染**内核的 `home.render_sidebar()` 侧边菜单（我的资料/消息/账号设置…，这些入口已在右上角头像下拉框里，`/home/*` 页面保持原样），主列与侧栏一律 9/3 栏；喵喵/奖章/算法展示页与算法评定页共用同一组组件，均为「左内容 + 右头像卡片」；`frontend/` 客户端 page 脚本；`locales/zh.yaml` i18n。
- `handler/draw.ts` 像素画编辑器 `/oi33/draw`（纯前端工具，无服务端数据）：模板 `templates/oi33_draw.html`，逻辑 `frontend/draw.page.ts`（全部状态在浏览器，历史/浮动面板位置存 localStorage），样式 `frontend/draw.css`；浮动工具面板可拖动/缩放/位置记忆，全局快捷键的清理靠 `NamedPage` 第三个回调（pjax 跳转前移除 document/window 监听）。

## 关键规则（容易踩坑）

- **domainId 注入**：handler 方法有 `@param/@query` 装饰器 → `domainId` 是第一个参数；没有装饰器 → 不注入。
- **权限**：公开 < 登录（PRIV_USER_PROFILE）< OI33 flag≥2（管理）< flag=3（行政）< PRIV_ALL。`realname_flag` 0 未认证/1 已认证/2 管理员/3 行政管理员。
- **未认证（flag<1）**：全站匿名化为 `UID <id>`（`mergeOi33Fields`+`anonymizeOi33Identity`）；禁止签到、地图、喂猫、买卖罐头、拍卖、合同、发喵喵、新建/编辑 paste、收猫粮、自动发放奖章。
- **用户列表渲染**：必须 `UserModel.getList`（带 `hasPriv()`，`components/user.html` 依赖）+ `getUserDataByUids` + `mergeOi33Fields`；禁用 `getListForRender` 渲染 user.html。
- **模板**：POST 表单必带 `csrfToken` 隐藏域；难度渲染一律走 `partials/oi33_difficulty.html`（0-8 洛古难度，默认遮罩、前端点击显示）。
- **审核引擎复用**：`handler/moderate.ts` 的 `runAiVerdict(uid, normalized, hash, cfg)`（规则+AI+缓存+预算熔断，fail-closed），喵喵/讨论区/简介共用；开关 `moderation_enabled`（默认开）。
- **算法掌握**：独立页 `/oi33/algorithm/user/:uid` 仅本人与 flag≥2 可见（handler 里判权限；资料页不内嵌该面板，只有侧边栏快捷入口）；`source:'teacher'` 只是展示信息、**不锁定**，学生提交自评即可覆盖；学生每个自然月只能提交一次整份自评（`oi33_user.algorithm_self_month`，原子占位、失败/无改动回滚），老师随时可改且不受限，可在学生面板上重置其本月额度；算法项目难度是 NOI 1-10 系数，别混用洛谷 0-8 的 `partials/oi33_difficulty.html`。详见下文「算法掌握程度（algorithm）」。
- **大猫（school-cat）**：地图格子 `catId` 编码为非负学校 id+1、特殊大猫负数原样，0=无大猫；**特殊大猫**用负数 `_id`（自定义 `name`，★ 不占数字名次，周奖励走管理员规则），是**唯一**的管理员大猫形式（旧 isAdminCat 手动设定已废弃，迁移会清除旧标记），仅 flag≥2 可搜索/绑定，flag≥3 可创建/改名；`transferSchoolCat`（flag≥3）把一只大猫的绑定用户/投喂历史/体重/领地格子整体转移到另一只并重算 territoryCount。周奖池 = 领地基础奖励（≥64 格起 1，翻倍 +1，封顶 12）× max(0, ⌊log2(体重)⌋−10)，按 ⌊log2(贡献)⌋ 权重最大余数法分配；结算完成后全体用户当前贡献与大猫体重各扣 5%（回滚不恢复）。奖章仅可售卖（`saleable`）或手动发放（含可升级系列的发放与等级变更）才自动发喵喵，自动规则发放与自动升级（OJ 成就奖章达标升级）、拍卖成交与合同转让都不发，管理员（flag≥2）不收到公告，且每个（用户, 奖章）只有一条公告（等级变更改写原帖文案），用户可删自己的喵喵。**地图分区**：帝国区=距边框 250 格环形（前/后 250 行、左/右 250 列），中央 500×500（坐标 250~749）为 00区（`CAT_MAP_CORE_MIN/MAX`，前端 `cat-can-arena.page.ts` 有同步副本）；分界线画成 00区外框的加粗「口」字，由地图「帝国边界」图层开关控制（默认开）。00区内某格与上下左右四邻全归属同一大猫即为「领地核心」（`fortressCatIdAt`），禁止绑定其他大猫的小猫移动/加入进入；染色形成核心时滞留其上的小猫被驱逐（`displaceFortressIntruders`）——绑定了大猫的传送到自己领地随机格，未绑定的传送到帝国区随机位置，驱逐走 `oi33/cat-map-change` player 广播。**路径规划**：`oi33_cat_map_plan`（`_id=uid`，一人一计划）存连续步骤（每步 = 相邻格 + 颜色码）、`cursor` 执行光标、`status`、`failReason` 与 `lockOwner/lockUntil` 执行租约；`oi33_cat_map_config`（`_id:'main'`）存可配置的 `planMaxSteps`（默认 10、夹取 1~50，绘图后台 flag≥3 修改）。`saveCatMapPlan`（replace/append）、`stopCatMapPlan`、`advanceCatMapPlan`（租约抢占 + 「小猫已在目标格」幂等恢复 + 位置校验 + 并发冲突重试 3 次）与 `runCatMapPlansDue` 都在 `model/cat-map.ts`，纯校验 `validateCatMapPlanShape` 便于单测；`index.ts` 只在 instance 0 上每 5 秒跑 `runCatMapPlansDue` 并广播事件（`{type:'plan', targetUid}` 由 `CatMapConnectionHandler.onMapChange` 按连接用户过滤，计划是私有数据）。前端（`frontend/cat-can-arena.page.ts`）画布只画 `cursor` 之后的未执行步骤、编号从 1 重新开始，全部执行完或计划已停止时不画圆圈，面板同样只列剩余步骤（`/oi33/arena/plan/state` 提供轻量计划视图，供「已到点但未收到推送」时兜底同步；两处响应都按 `updatedAt` 拒绝更旧的视图）。手动 move/color 与管理员强制迁移成功后都会立刻停止计划；自动停止用 `MessageModel.send` 私信本人。每步复用 `moveCatMapPlayer` + `setCatMapCellColor`，所以费用、冷却、贡献、驱逐与日志与手动操作完全一致。

## 个人简介（bio）AI 审核

bio 是 Hydro 核心字段，编辑走 core `/home/settings/account`，本插件用 `handler/before|after/HomeSettings#post` 钩子介入（`handler/bio.ts`）：改动有 **2 小时冷却**（`bio_edited_at`）；变更后写 `bio_status:'pending'`+`bio_hash` 并后台 AI 审核：pass→approved；block/review/出错→rejected 隐藏并私信通知本人（含分类与理由；人工驳回只发「人工审核未通过」）。所有判定记入 `oi33_ai_moderation`（`kind:'bio'`，见 `/oi33/ai/moderation` 结果列表），review/出错进人工队列，管理员 approve/reject 直接翻转 bio 状态（哈希守卫，不会覆盖更新的编辑）。**显示规则**（`mergeOi33Fields` 算 `udoc.bio_visible`）：flag≥1 且 `bio_status==='approved'` 且 `bio_hash===bioHashOf(当前 bio)`——哈希不符（如 OAuth 回写）自动隐藏。展示点：资料页「个人简介」卡片（未过审时本人见提示，`after/UserDetail` 钩子同时清空 JSON 里的 bio）、首页 ranking partial。初始批量审查：管理页 `/oi33/ai/bio-review`（flag≥2，幂等可重跑，进度存 `oi33_ai_batch` 的 `_id:'bio_review'`）。

**队列记录与「已被修改」错误**：审核记录绑定的是它当时判定的那一版简介（`content`/`contentHash`），只有该版本仍是**当前** bio 时才可 approve/reject。判定统一走 `model/moderate.ts` 的 `bioQueueState(entry, live)` → `'ok'`（可审）/ `'text'`（文本相同但 `bio_hash` 漂移，例如被旧批量审查覆盖；决定仍可作用于该文本并改写哈希，否则「通过」后简介仍然不显示）/ `'stale'`（用户已改过简介或账号不存在，永远无法应用）。`live` 由 `getLiveBio`/`getLiveBios`（`model/user.ts`，直读 core `user` 集合，不走 User 缓存）取得。**旧的死循环**：`'stale'` 记录以前直接抛 `该个人简介已被修改，不能处理旧审核记录`，两个按钮都失败且记录永远 pending（`modListPending` 只取最旧 200 条），队列被堵死；现在改为关闭为 `'stale'` 状态（`modExpireEntries`，只动队列不动用户简介状态），审核页给这类记录打「简介已变更，仅可标记失效」标记并把按钮换成「标记失效」，另有一键「清理失效的简介记录（N）」（`expireStaleBioEntries`）。预防：`bioMarkEdited` 会顺带把该用户此前的 pending bio 记录关闭为 stale（新的编辑自带一条新记录），批量审查每个用户在 AI 调用前后各重读一次 live bio，改动中的用户跳过，避免用旧文本覆盖 `bio_hash`。

## 算法掌握程度（algorithm）

算法掌握面板（独立页 `/oi33/algorithm/user/:uid`）展示该学生每个算法知识点的掌握程度；仅**本人**与 **OI33 flag≥2 老师**可见（`AlgorithmUserHandler` 判权限，其余访客 403）。资料页不内嵌这张卡片，只保留侧边栏「快捷跳转」入口。展示页与评定页分开：`/oi33/algorithm/user/:uid` 展示页只读、把知识点渲染成按掌握度着色的标签云并按「级别 → 板块 → 子板块」逐层折叠；`/oi33/algorithm/user/:uid/edit` 评定页每行一组四档单选框、可按评价与关键词筛选。评定档位只有 4 档：0 没学（灰）/ 1 不会（红）/ 2 不熟（黄）/ 3 熟练（蓝），色号取自洛谷难度评级；**没有记录的知识点按「没学」显示**（不再有独立的「未评定」档位，前端也不再有 `-1` 选项；后端 `algorithmSetLevels` 仍接受负值来删除记录，供程序化调用）。**学生每个自然月（Asia/Shanghai）只能提交一次自评**，提交后整月只读；**老师（flag≥2）随时可改任意学生，不受月度限制**。

- **数据**：`oi33_algorithm_item`（`_id` = 规范化后的 NOI 大纲编号，如 `1.1.12`（原 `2.1.1-12`）；规则见 `normalizeOutlineId`：去掉文档章节号 `2.`、条目序号 `-N` 改成 `.N`，级别/板块/子板块 id 也一并去掉 `2.`；自定义项目为 `custom-*`；字段 `levelId/levelName/sectionId/sectionName/subsectionId/subsectionName/difficulty(0-10)/order/enabled/source('noi2025'|'custom')/idVersion`（`idVersion:2` = 已规范化；迁移只处理 `idVersion ≠ 2` 的行，因为规范化后的提高级 id 也以 `2.` 开头，再跑一次会被误剥））、`oi33_user_algorithm`（`(uid,itemId)` 唯一，`level` 0-3，`source` `'self'|'teacher'` 仅表示当前值由谁录入、不是权限，`updatedBy/updatedAt`）、`oi33_algorithm_config`（`_id:'main'`，`selfEdit`）、`oi33_user.algorithm_self_month`（'YYYY-MM'，学生本月已用掉的提交额度）+ `algorithm_self_at`。
- **默认内容**：`model/noi-outline-2025.json` 随插件内置（NOI 2025 大纲，3 级/12 板块/260 项，难度 1-10；官方「基础知识与编程环境」板块已剔除，常量 `ALGORITHM_DROPPED_SECTION`）。`/oi33/migrate` 的 Step 15 在集合为空时导入（`algorithmEnsureOutlineImported`，幂等），Step 17 用 `algorithmDropBasicsSection` 删除历史导入过的该板块（连同其评定，幂等）；后台「算法项目管理」`/oi33/algorithm/manage`（flag≥2）可再次导入/覆盖、增删改、启用停用、按级别批量启停、调整排序，并提供「允许学生自评」总开关。导入默认只补缺失项，勾选「覆盖」才改写已有项目的名称/分组/难度/排序（`enabled` 始终保留）。
- **覆盖规则**：`source` 只是展示信息（🔒「教师录入」徽标），**不是权限**——学生提交自评即可覆盖教师录入的值，覆盖后该行 `source` 变回 `'self'`。老师设置他人写 `'teacher'`，老师编辑自己的面板写 `'self'`。
- **月度额度**：`algorithmMonthKey()`（Asia/Shanghai 的 'YYYY-MM'）+ `algorithmNextUpdateDate()`（下月 1 日）。学生提交时 `algorithmClaimSelfQuota(uid, month)` 先原子占位（条件更新 `{_id: uid, algorithm_self_month: {$ne: month}}`，占不到即本月已用过并报「下次可更新」）；写库失败或「没有任何改动」时 `algorithmReleaseSelfQuota(uid, month)` 回滚，绝不白扣额度。老师可在学生面板上点「重置该学生本月额度」（`/oi33/algorithm/reset-month` → `algorithmResetSelfQuota`）让学生本月再交一次；老师自己的写入从不消耗额度。
- **写路径**：`algorithmSetLevels(uid, entries, operator, {onlyChanges})`：提交 `0`-`3` 为评定档位，非数字或 `>3` 夹取到 0-3，负值 = 删除该条记录（后端保留该能力供程序化调用，前端已无此选项）；`onlyChanges` 用客户端提交的**渲染时原值** `orig_<idx>`（缺失时退回库内当前值）作为基线，等于基线就跳过——没有记录的行渲染为「没学」(0)，原样提交即跳过，因此既不会凭空写入记录、也不会用久开页面的旧值覆盖老师期间的新修改。整份面板是**单表单批量提交**（页面上唯一的 `#alg-form`，每行 4 档单选框和 hidden 都用 `form="alg-form"` 关联，字段 `lv_<idx>` + `id_<idx>` + `orig_<idx>`，`idx` 由 `algorithmProfileView` 全局分配），有脚本时提交前确认、脏标记与实时统计，无脚本时直接整表提交；返回 `{updated, cleared, skipped, unchanged}`。
- **统计与前端**：`algorithmComputeStats` 给出 4 档计数与掌握度 `round(100*Σlevel/(3*total))`（没有记录的按没学计；`stats.rated` 仍在但界面不再展示「已评定/未评定」），`algorithmProfileView` 另按级别 / 难度系数出进度，并给每个板块、子板块附统计；`frontend/algorithm-mastery.page.ts` 用 document 级委托做按评价 + 关键词筛选、展开收起、统计实时重算、脏标记/提交按钮与 `beforeunload` 提醒（表单真正提交时先清掉脏标记，否则浏览器会在保存跳转时弹出「你所做的更改可能未保存」；pjax 安全，无 `NamedPage`；只对 `data-alg-can-edit=1` 的评定页生效）；`templates/partials/oi33_algorithm.html` 的 `display()`（展示页：标签云 + 逐层折叠）、`editor()`（评定页：四档单选框 + 筛选）与 `style()` 被展示页与评定页共用。
- **难度**：这里显示的是 NOI 大纲难度系数 1-10，**不要**复用洛谷 0-8 的 `partials/oi33_difficulty.html`。

## patches.ts 清单

`getList`/`getListForRender`/`getById`/`User.private` 注入 oi33 字段并匿名化；`HomeHandler.getCheckin/getCountdown` 首页数据；`handler/after/*` 兜底匿名化；Bearer `33tok_` 只读 token 鉴权（跳过 `/oi33/oauth/*`）；nunjucks `getTemplate` 启动竞态守卫。

## 安装

```bash
hydrooj addon add /path/to/oi33 && pm2 restart hydrooj
# 访问 /oi33/migrate 跑迁移（幂等）
```
